@model IEnumerable<Course>
<!-- Latest property -->
<section>

</section>
    <section>
        <div class="aa-latest-property-area">
            <div class="aa-title">
                <h2>Latest Courses</h2>
                <span></span>
            </div>
            <div class="aa-latest-properties-content">
                <div class="row">
                    @foreach (var item in Model)
                    {
                        <div class="col-md-4">
                            <article class="aa-properties-item">
                                <a href="#" class="aa-properties-item-img">
                                    <img src=@Url.Content("~/img/item/" + item.courseimg) asp-append-version="true" />
                                </a>
                                <div class="aa-tag for-sale">
                                    @item.Venu
                                </div>
                                <div class="aa-properties-item-content">
                                    <div class="aa-properties-info">
                                        <span>Start Date</span>
                                        <span>@item.StartDate.ToShortDateString()</span>
                                        <span>Start Time</span>
                                        <span>@item.StartTime</span>
                                    </div>
                                    <div class="aa-properties-about">
                                        <h3><a asp-action="CourseDetails" asp-controller="Home" asp-route-id="@item.CourseId">@item.CourseName</a></h3>
                                        <p>@item.CourseDesc.Substring(10, 1) ...</p>
                                    </div>
                                    <div class="aa-properties-detial">
                                        <span class="aa-price">
                                            @item.Price -jod
                                        </span>
                                        <a asp-action="CourseDetails" asp-controller="Home" asp-route-id="@item.CourseId"> class="aa-secondary-btn">View Details</a>
                                    </div>
                                </div>
                            </article>
                        </div>
                    }
                </div>
            </div>
        </div>
    </section>

    <section>

    </section>
    <!-- / Latest property -->
